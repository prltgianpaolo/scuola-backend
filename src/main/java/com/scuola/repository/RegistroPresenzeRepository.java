package com.scuola.repository;

import com.scuola.model.RegistroPresenze;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegistroPresenzeRepository extends JpaRepository<RegistroPresenze, Long> {
}
